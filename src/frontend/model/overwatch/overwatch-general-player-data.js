export default class OverwatchGeneralPlayerData {
    constructor(battleTag, level, portraitUri, levelFrameUri, star) {
        this.battleTag = battleTag
        this.level = level
        this.portraitUri = portraitUri
        this.levelFrameUri = levelFrameUri
        this.star = star
    }
}